name: Rebuild profile assets

on:
  schedule:
    - cron: "23 2 * * *" # daily at 02:23 UTC (10:23 MYT)
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - profile.toml
      - scripts/**
      - .github/profile-3d.json
      - .github/workflows/build.yml

permissions:
  contents: write

concurrency:
  group: rebuild-profile-assets
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: scripts/requirements.txt

      - name: Build typographic cards
        run: |
          pip install -r scripts/requirements.txt
          python scripts/build.py
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Draw the 3D contribution map
        uses: yoshi389111/github-profile-3d-contrib@v0.9.3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          USERNAME: ${{ github.repository_owner }}
          SETTING_JSON: .github/profile-3d.json

      - name: Commit refreshed assets
        run: |
          git add -A assets data profile-3d-contrib
          if git diff --cached --quiet; then
            echo "Assets already up to date."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "chore: refresh profile assets"
          git push
